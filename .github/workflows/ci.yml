name: Testes Automatizados (CI)

on:
  push:
    branches: [ "main", "develop" ] 
  pull_request:
    branches: [ "main" ] 

jobs:
  build-and-test:
    runs-on: ubuntu-latest 
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpassword
          POSTGRES_DB: gestaopro_test_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: 1. Checkout do Código
      uses: actions/checkout@v4

    - name: 2. Configurar Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 3. Instalar Dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 4. Rodar os Testes
      run: |
        pytest --cov=app
      env:
        DATABASE_URL: postgresql://postgres:34251311@localhost:5432/gestaopro_test_ci
        SECRET_KEY: "J!lans@DflkaEs.Lo548,24a.sD5!As48as!!d42sa,.54dF847-24ASf4857aSdf4566GFg4"
        ALGORITHM: "HS256"
        ACCESS_TOKEN_EXPIRE_MINUTES: "30"